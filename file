def return_value
pipeline
{
	agent any 
	
	stages
	{
		stage('python')
		{
			steps
			{
			git 'https://github.com/esmy1990/locust-experiments.git'
			script
				{
			
 				def status = bat(returnStderr: true, script: "g.py > merge.txt")
				echo ("$status")
				if (status !=0 ) {
  				currentBuild.result = 'FAILED'
  
				}

			}
			}
		}
	}
}
